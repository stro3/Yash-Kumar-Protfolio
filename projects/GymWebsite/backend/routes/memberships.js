const express = require('express');
const router = express.Router();

// Placeholder for membership routes
router.get('/', (req, res) => {
  res.json({ message: 'Memberships routes - coming soon' });
});

module.exports = router;